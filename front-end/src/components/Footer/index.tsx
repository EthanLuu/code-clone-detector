import { Layout } from 'antd';
import styles from './index.less';

export const Footer: React.FC = () => {
  return <Layout.Footer className={styles.container}>
    Created by EthanLoo
  </Layout.Footer>
}
